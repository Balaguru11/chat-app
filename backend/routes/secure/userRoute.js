const express = require("express");
const router = express.Router();

const { userSignout } = require("../../controllers/userController");

// router.get("/signout", userSignout);
module.exports = router;
